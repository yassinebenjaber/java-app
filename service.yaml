apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80        # Service port exposed
      targetPort: 8080 # Container port from deployment.yaml
  type: NodePort
